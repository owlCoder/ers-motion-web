# Tapiz Boards screenshots

This directory contains real Tapiz Boards UI screenshots used by the ERS practicum. The images are captured from the demo environment and are intentionally used instead of reconstructed mockups.
